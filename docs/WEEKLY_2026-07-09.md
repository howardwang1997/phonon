# 周报 · 2026-07-09 — 支线收尾:家族 (L) 全完成 + 统一模型 B2d 落地 + A2 诚实边界

**分支** `smearing-kink-ml` · **主题** 支线三件未跑实验补齐:① 家族 6 材料 (L)-SSCHA 全完成(数据在 2060,补同步+分析+图);② A2 NbS₂ 4×4 收敛诚实测试(发现 3×3 vs 4×4 模型深度差);③ B2d 统一模型(一个 Path-P backbone 同时出 (E)+(L) kink)落地。

---

## TL;DR
- **家族 (L) 全完成**:6 CDW 材料 SSCHA 软模(T_lat)都跑了。**1T(VSe₂/TiSe₂)深软模 heal 在 T_CDW(110/200K)= 晶格驱动**;**2H(NbSe₂/NbS₂/TaS₂/TaSe₂)3×3 下 ~0 惰性 = 电子驱动**。origin 分流坐实(`family_L_crossover.png`)。
- **A2 诚实测试(NbS₂ 4×4)**:4×4 SSCHA 给 −264 cm⁻¹ @20K(3×3 给 ~0)→ **3×3 把 (L) 软模抹平了**。但 DFT fc₂ bare 软模 3×3(−63)/4×4(−69)**相近** → 差异来自 **Path-P 模型双井深度**(4×4 微调学了更深井),非 DFT 收敛。诚实结论:2H (L)-惰性是**模型/超胞敏感**的,−264 待 DFT 直接验证(真深井 vs 过拟合)。**A2 达到目的:暴露了诚实边界。**
- **B2d 统一模型(支线交付物)**:一个 **Path-P backbone** + Friedel 长程项 → (E) kink 从长程项(T_el)、(L) kink 从 backbone 非谐(T_lat),两轴**塌缩到同一 healing 律**(p=2.55,q=1.47,MAE 0.060)。统一概念落地。
- **NbSe₂ NQ=4 EPW**:DFPT 4×4 仍在跑(Box A,ph.x 8 核,慢,ETA ~7/10)。

---

## 1. 家族 (L)-SSCHA 全完成(补同步 + 图)

6 材料 SSCHA 软模(T_lat)(数据原在 2060,本次同步到本地 + 出图 `family_L_crossover.png/csv`):

| 材料 | polytype | (L) 软模 20K | crossover | origin |
|---|---|---|---|---|
| 1T-VSe₂ | 1T | −360 cm⁻¹(50K 最深 −412)| **110K = T_CDW** | 晶格/非谐 ✓ |
| 1T-TiSe₂ | 1T | −552 cm⁻¹ | **200K = T_CDW** | 晶格/非谐 ✓ |
| 2H-NbSe₂ | 2H | ~0 | n/a | 电子 ✓ |
| 2H-NbS₂ | 2H(3×3)| ~0 | n/a | 电子(3×3,见 §2)| 
| 2H-TaS₂ | 2H | ~0 | n/a | 电子 ✓ |
| 2H-TaSe₂ | 2H | ~0 | n/a | 电子 ✓ |

**头条**:两个 1T CDW 的 (L) 软模都**精确在 T_CDW 处 heal**;4 个 2H 在 3×3 下 (L) 惰性。origin 按 polytype 分流坐实(第二周报的发现,本周补全家族 + 图)。

---

## 2. A2 诚实测试:NbS₂ 4×4 — 3×3 抹平了 (L) 软模,但根因是模型双井深度

A2 跑 NbS₂(2H 中 bare 软模最深 −103 cm⁻¹,最强测试)4×4 Path-P → 微调 → SSCHA:

| T_lat | NbS₂ 3×3 SSCHA | NbS₂ 4×4 SSCHA |
|---|---|---|
| 20 K | ~0 | **−264.6 cm⁻¹** |
| 100 K | ~0 | ~0 |
| 200/300 K | ~0 | ~0 |

→ 4×4 暴露了 3×3 抹平的低 T (L) 软模(heal 在 ~100K,NbS₂ 无 CDW)。

**但诊断(关键诚实点)**:DFT fc₂ **bare** 软模 3×3=−63 / 4×4=−69 cm⁻¹ **相近** → 3×3 vs 4×4 的 SSCHA 巨差**不是 DFT 收敛**,而是 **Path-P 微调模型的双井深度**:4×4 模型学了更深双井 → SSCHA 自由能 Hessian 软模更深。

**含义(写文章须交代)**:
1. 2H "(L)-惰性"的结论是 **3×3 Path-P 模型层面**的,4×4 模型给出更深软模 → **模型/超胞敏感**。
2. −264 cm⁻¹ 是**模型输出**,不是 DFT 直接测量;**待 DFT 直接验证**(4×4 双井 DFT 扫描 或 DFT-MD-TDEP)才能定真深井 vs 过拟合。
3. 但 1T vs 2H 的**对比仍稳**:1T (L) 软模深 5–8×(−360/−552)且 heal **在 T_CDW**;2H DFT bare 软模浅(−65)且 NbS₂ 4×4 heal 在 100K(无 CDW)。
4. 2H CDW 材料(NbSe₂/TaS₂/TaSe₂)的 3×3 (L)-惰性需 4×4 复核为严谨(future work,V100 DFT ~2–3h/材料)。

A2 达到设计目的:暴露了 (L)-SSCHA 的模型/超胞敏感诚实边界。

---

## 3. B2d 统一模型(支线交付物):一个 backbone 出两轴 kink

`b2d_unified_kink.py`(2060):一个 **Path-P VSe₂ backbone** + T_el-条件化 Friedel 长程项 = FriedelMACE 模型。

- **(E) 轴**:FriedelMACE(Path-P, Friedel(T_el)) 谐振 fc₂ → 软模(T_el)。Friedel LR 幅度 B(T_el) 随 T_el 衰减 → kink 熔化。
  - 789K: −2.04 THz(DFT −2.18);3158K: −0.006;4737K(B=0): −0.131(backbone bare 残留,VSe₂ 已知 deploy 问题)。
- **(L) 轴**:Path-P backbone 的 SSCHA 自由能 Hessian(B=0)→ 软模(T_lat)。深度 −12.35 THz @50K,heal 在 T_CDW=110K。
- **统一塌缩**:两轴归一化到 reduced-T T/T\* → 同一 healing 律 **f(x)=(1−xᵖ)q**,**p=2.55, q=1.47, MAE 0.060**(B2 step-2 用 DFT (E) 数据得 p=4.44,q=3.00,MAE 0.026;B2d 用 deploy (E) 切片,故 (p,q) 不同)。

→ **统一概念落地**:一个 Path-P backbone,(E) kink 从长程项、(L) kink 从 backbone 非谐,两轴同 healing 形式。`b2d_vse2.csv/.png`。

**诚实边界**:(E) 切片是 Friedel deploy(有 VSe₂ −0.131 bare 残留,即 0.717 那个已知问题);(L) 切片干净。统一是**概念级 + reduced-T 形式级**,不是 drop-in 2D 部署模型(B2 step-3 已示 B 幅度律直接部署为负)。

---

## 4. NbSe₂ NQ=4 EPW(Box A,跑中)

DFPT 4×4 金属(16 q-points)在 Box A CPU 跑(ph.x 8 核,GPU 闲因 QE ph.x 是 CPU)。慢,ETA ~7/10。NQ=4 与 CDW q=1/3 incommensurate → 干净 λ(不发散)。csv 仍只有 header(DFPT 未完)。监控,不动。

---

## 5. 三机状态(本轮结束)

| 机器 | 状态 |
|---|---|
| 2060 | A2 4×4 done + B2d done → **空闲** |
| Box A | NbSe₂ NQ=4 DFPT 跑中(CPU);GPU 闲 |
| Box B | **空闲** |

---

## 6. 支线完成度 + 还差什么

**支线目标**(一个 MLIP+长程模型复现 kink(T_el,T_lat)):
- ✅ (E) kink(T_el) via MLIP+长程:家族准(0.011–0.16;VSe₂ 0.717 reframe)。
- ✅ (L) kink(T_lat) via MLIP(Path-P+SSCHA):家族全完成(1T heal @T_CDW)。
- ✅ 统一 reduced-T 律:B2 step-2 找到 + 交叉验证(p=4.44,q=3.00)。
- ✅ 统一模型 B2d:一个 backbone 出两轴,同 healing 形式(本轮)。
- ⚠️ 诚实边界:A2 示 (L)-SSCHA 模型/超胞敏感;B2d (E) 切片有 VSe₂ deploy 残留;统一律非 drop-in B 幅度律(step-3 负)。

**还差(可选/严谨)**:
1. **2H 4×4 复核**(NbSe₂/TaS₂/TaSe₂):验 3×3 (L)-惰性是否模型浅。V100 DFT ~2–3h/材料。
2. **A2 −264 的 DFT 直接验证**(4×4 双井扫描 / DFT-MD-TDEP):定真深井 vs 过拟合。gated。
3. **B2d Phase-2**:2D SSCHA(FriedelMACE 在 (T_el,T_lat) 网格)出 kink 2D 面(~45min 2060)。
4. **NbSe₂ NQ=4** 干净 λ + Family EPW stage 2:(E) 第一性原理背书。
5. **B3 DFT-direct VSe₂ (L) 仲裁**:金标准,gated。

---

## 7. 产物(本轮新增,待提交)

- 脚本:`scripts/smearing_kink/{plot_family_L, b2d_unified_kink, run_A2_2060_nbs2_4x4_sscha, run_B2d_2060}.py/.sh`;`plot_origin_map.py` 加 TiSe₂。
- 数据/图:`results/smearing_kink/{family_L_crossover.png/.csv, b2d_vse2.png/.csv, origin_map.png/.csv(更新)}`;`results/td_phonon/{NbS2_4x4.csv, 1T-VSe2_L.csv, 1T-TiSe2_L.csv, 2H-TaS2_L.csv, 2H-TaSe2_L.csv}(同步)`。
- 文档:本 weekly。

## 一句话
支线三件未跑实验补齐:家族 (L) 全完成(1T 晶格 / 2H 电子坐实)、A2 诚实测试(NbS₂ 4×4 暴露 (L)-SSCHA 模型/超胞敏感,DFT bare 相近 → 待 DFT 验证)、B2d 统一模型(一个 backbone 出 (E)+(L) 两轴 kink,同 healing 形式)。支线核心交付物落地,剩可选严谨项。
