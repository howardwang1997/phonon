# Repository Instructions

## 当前研究目标

- 用 MLIP 加上长程微调法，把声子谱计算准确，作为现阶段的目标。
- 当前 graphene 工作以定量复现有限晶格温度下的 Kohn anomaly 为验收重点；仅确认结构稳定、没有软模或没有 CDW，不视为完成该目标。

## Python 环境

- 优先使用项目的 Conda 环境 `phonon`。
- 运行 Python 时使用 `conda run -n phonon python ...`，不要创建 venv 或 virtualenv。

## 远程机器连接

- 连接 V100-A、V100-B 和 RTX 2060 时统一经过 Tailscale 网络；优先使用 `tailscale ssh`。
- macOS App Store 版客户端不支持 `tailscale ssh`，或 `tailscale nc` 的用户态通道不可用时，可以让 OpenSSH 通过系统 Tailscale TUN 连接节点的 `100.x` 地址或 MagicDNS 名称。此时目标必须是 Tailscale 地址，不能用公网 IP 提交实验或传数据。
- 远程复制或部署也应通过 Tailscale 网络完成。
- 如果远端节点确实从 tailnet 离线，可以临时通过该机器的公网 IP 登录，但只用于重新启动或修复 Tailscale；恢复后立即改回 Tailscale 通道，不要继续用公网 IP跑实验或传数据。

## 周报与科研写作风格

- 中文周报、研究记录和论文说明使用自然、直接、具体的科研表达，减少模板化的“AI 味”。
- 优先写清楚“做了什么、得到什么数值、这些数值支持什么结论”；结论强度必须与数据相符。
- 避免把“一句话”“诚实边界”“诚实留白”“铁证”“坐实”“碾压”“打脸”“红旗”“捏造”等用作固定话术。通常改为“摘要”“适用范围”“限制与说明”“结果表明”“数值伪影”等中性表达。
- 不要频繁使用“不是 A，而是 B”、连续破折号、口号式总结、夸张形容词、密集加粗或勾选符号。只有确实有助于阅读时才使用强调。
- “预注册”只用于确实存在正式预注册的场景。内部 holdout 实验应写成“在 holdout 计算前固定模型、测试点和验收标准”，或简称“事先确定的测试方案/验收标准”。
- 区分定量结果、定性结果和后续工作。无法由当前数据支持的内容直接写入“适用范围”或“限制与说明”，不使用自我辩护式措辞。
- 保留必要的英文术语时，中文句子仍应通顺；能用普通科研中文说清楚时，不堆叠英文缩写或项目管理术语。

## 图表表达

- legend、colorbar 和注释不得遮挡数据；优先放在坐标轴外或图中空白区域。
- Fermi–Dirac 电子展宽默认直接写 `smearing/degauss (Ry)`，不要换算为 `T_el (K)`，除非用户明确要求。
- 晶格温度仍使用 K，并明确区分 lattice temperature 与 electronic smearing。
